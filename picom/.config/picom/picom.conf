backend = "glx";
vsync = true;

fading = true;
fade-delta = 3;

rules = (
  { match = "class_g = 'St' || class_g = 'GoldenDict-ng'"; opacity = 0.9; },
  { match = "class_g != 'St' && class_g != 'GoldenDict-ng'"; blur-background = false; },
)

blur:
{
  method = "dual_kawase";
  strength = 2;
};

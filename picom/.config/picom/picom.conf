backend = "glx";
vsync = true;

fading = true;
fade-delta = 3;

rules = (
  { match = "class_g = 'St' || class_g = 'Code'"; opacity = 0.9; },
  { match = "class_g != 'St' && class_g != 'Code'"; blur-background = false; },
)

blur:
{
  method = "dual_kawase";
  strength = 2;
};
